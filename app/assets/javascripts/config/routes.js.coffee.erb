@Obra.config ($locationProvider, $routeProvider) ->
  $locationProvider.html5Mode(true)
  $routeProvider.when("/",
    controller: "IndexController"
    template: JST["home/index"]
    redirectTo: (current, path, search) ->
      if search.goto
        path + search.goto
      else
        path

  )